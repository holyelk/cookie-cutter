apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-service
spec:
  rules:
  - host: chart-example.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 80
